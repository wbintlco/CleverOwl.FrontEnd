@model IEnumerable<RLI.EntityFramework.EDM.Cycle>
@using RLI.Common.Managers
@{
    Dictionary<string, string> Locale = ViewBag.Locale;

}

<div id="CycleTableDiv" class="col-lg-12" style="min-height:320px;">
    <table id="m-table" class="table table-striped- table-bordered table-hover table-checkable dataTable no-footer dtr-inline" role="grid">
        <thead>
            <tr>
                <td></td>
                <td>@Html.Raw(UtilitiesManager.GetFieldLabel(Locale, "CyclesPageCycle1"))</td>
                <td>@Html.Raw(UtilitiesManager.GetFieldLabel(Locale, "CyclesPageActions"))</td>
            </tr>
        </thead>
        <tbody>
            @foreach (var cycle in Model)
            {
                <tr>
                    <td>@cycle.CycleKey</td>
                    <td>@cycle.Cycle1</td>
                    <td></td>
                </tr>
            }
        </tbody>
    </table>
</div>



